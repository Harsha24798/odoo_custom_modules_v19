<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron Job: Extend Offer Deadlines -->
        <record id="cron_extend_offer_deadline" model="ir.cron">
            <field name="name">Extend Offer Deadlines</field>
            <field name="model_id" ref="model_estate_property"/>
            <field name="state">code</field>
            <field name="code">model._cron_extend_offer_deadline()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">False</field>
        </record>
    </data>
</odoo>

